package characters

import scalafx.scene.shape.Rectangle
import scalafx.scene.paint.Color._

class Bob extends Character(

  name = "Bob",
  baseHp = 100,
  baseMp = 100,
  baseAtkDmg = 100,
  baseMgcDmg = 100,
  baseSpeed = 100
  
) {

  
}
